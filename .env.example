# Database Configuration
DATABASE_URL=postgresql+asyncpg://gdpr_rag:devpassword@localhost:5432/gdpr_safe_rag

# For SQLite (lightweight alternative)
# DATABASE_URL=sqlite+aiosqlite:///./gdpr_safe_rag.db

# Logging
LOG_LEVEL=INFO

# PII Detection
PII_DETECTION_LEVEL=strict
# Options: strict, moderate, lenient

# Audit Settings
AUDIT_RETENTION_DAYS=2555

# Optional: Encryption key for PII mapping (base64 encoded)
# PII_ENCRYPTION_KEY=your-base64-encoded-key-here
